# Real backend base URL — talks to the FastAPI backend (see src/lib/api.js).
# VITE_API_URL=http://localhost:8000/api/v1

# Mock LMS public URL (mục 6.6) — "Mở Mock LMS" link in Topbar. Must be
# "localhost", not "127.0.0.1" (cookie host-scoping, see mock-lms/README.md).
# VITE_MOCK_LMS_URL=http://localhost:9000

# Dev/CI-only tooling — never installed in the production Docker image
# (Dockerfile only installs requirements.txt). Install both locally:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

ruff==0.16.4
pytest==9.1.1
pytest-asyncio==1.4.0

# Load testing (not needed for the app itself — see loadtest/README.md)
locust==2.46.3

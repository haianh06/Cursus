# CHANGELOG — Cursus

Lịch sử thay đổi của `PROJECT_CONTEXT.md`. Tách ra file riêng ngày 15/08/2026 (đợt 6) vì file chính đã quá dài (~800+ dòng), gộp cả product truth + backlog + lịch sử thay đổi khiến AI coding assistant dễ đọc nhầm backlog cũ thành hiện trạng — theo góp ý từ bản phản biện độc lập lần 2.

---

- File `PROJECT_CONTEXT.md` được tổng hợp lần đầu ngày 13/08/2026 từ các tài liệu gốc (PRD/SRS/đối thủ/lịch trình) — các file gốc đó đã xoá sau đó, nội dung còn giá trị đã gộp vào đây.

- **Đợt 1-3 (15/08/2026):** gộp docs còn 2 file sống chính (`PROJECT_CONTEXT.md`, `docs/FRONTEND_SPEC.md`); bỏ khung Gate 2, chốt deadline duy nhất 23/08/2026; đổi tư duy tài liệu từ "đủ để qua vòng chấm" sang "hoàn thiện toàn bộ Cơ bản + Nâng cao".

- **Đợt 4 (15/08/2026) — hoàn thiện chức năng theo role qua phiên hỏi-đáp trực tiếp với founder, không còn giới hạn theo khung thời gian:**
  1. Giảng viên chỉ thấy lớp/các lớp được Admin gán khi mời — không tự chọn xem lớp khác hay toàn trường.
  2. Chỉ Admin (Phòng đào tạo) gửi được lời mời tạo tài khoản mới, kể cả cho giảng viên.
  3. Sinh viên không tự chọn môn học — enrollment gán sẵn theo dữ liệu thật, giữ đúng định vị cạnh tranh.
  4. Risk threshold cố định trong code (quyết định sau này bị đảo ngược ở đợt 5).
  5. Tab "Chính sách AI" ở Admin Console: Admin bật/tắt được rule guardrail qua UI (không sửa nội dung pattern).
  6. Canvas LMS giả lập **có giao diện riêng thật**, không chạy ngầm — theo đúng yêu cầu PLO 5 (tích hợp hệ thống ngoài phải chứng minh được).
  7. Chỉ 1 tổ chức (FPT) trong phạm vi demo — cơ chế multi-tenant vẫn còn nguyên trong code.
  8. Sinh viên năm 4 ("Minh") dùng chung 1 luồng Plan-Do-Reflect, không có logic riêng cho đồ án tốt nghiệp.
  9. Bổ sung lỗ hổng đăng ký tự do được role giảng viên/admin vào danh sách việc cần làm — ưu tiên #1.
  10. Nối số liệu eval cũ (guardrail 30/30, RAG citation 24/25) với bug PROMPT_PATH — đánh dấu không đáng tin.
  11. Trỏ rõ tới các file RLS đã chuẩn bị sẵn (`rls-migration-plan.md`, migration SQL, `tenant_scope.py`).
  12. Thêm access-control fixtures — test riêng cho việc không escalate role và không cross-class leak.

- **Đợt 5 (15/08/2026) — chốt các gap I/O còn mơ hồ + nghiên cứu chuẩn ngành, để bắt tay code UI/UX ngay:** đảo ngược quyết định risk threshold (giờ Admin chỉnh được qua UI, có audit log); chốt cách ghi `actual_minutes` ở Do (nhập tay sau khi hoàn thành, không dùng timer); liên kết "Yêu cầu hỗ trợ" ở Reflect với alert giảng viên; thêm chính sách re-alert; thêm ví dụ Socratic mẫu thật; thêm xử lý xung đột đồng bộ Canvas ảo; nâng mục tiêu load test 1.000→2.500; chốt stack eval RAGAS+DeepEval với ngưỡng pass/fail cụ thể; bổ sung Starfish/EAB Navigate360/Khanmigo vào bảng đối thủ; trích dẫn cơ sở lý thuyết Zimmerman SRL cho Plan-Do-Reflect; thêm 3 mục OWASP LLM Top 10 vào threat model; xác nhận lại stack deploy Railway+Supabase+Vercel có so sánh với Cloudflare/Render/Firebase/Hostinger.

- **Đợt 6 (15/08/2026) — sửa toàn bộ theo bản phản biện độc lập lần 1 (điểm 7/10), áp dụng đúng thứ tự P0:** sửa mâu thuẫn "ẩn danh" (Lecturer định danh SV trong lớp được gán, chỉ Admin xem tổng hợp ẩn danh); hạ claim "multi-agent" xuống "stateful AI workflow/orchestrated pipeline"; sửa thuật ngữ LTI 1.3 (phân biệt OIDC/JWT/JWKS launch với NRPS/AGS), đổi ưu tiên sang REST API mock trước, LTI là stretch goal; đánh dấu eval cũ là INVALID/SUPERSEDED; đổi ngưỡng RAGAS thành "provisional internal thresholds"; viết đầy đủ data retention/deletion theo 4 mức; thêm versioning cho risk policy; đổi RLS "an toàn tạm thời" thành "known high-risk gap"; thêm evidence/nguồn cho claim đối thủ; viết lại mục việc-cần-làm theo P0/P1/P2; thêm ma trận guardrail 4 nhóm; tách citation eval thành 8 khía cạnh; thêm feasibility checks cho Planner eval; mở rộng bảng quy mô eval theo tầng.

- **Đợt 7 (15/08/2026) — sửa theo bản phản biện độc lập lần 2 (điểm 7.8/10, chuẩn "sản phẩm hoàn chỉnh", tự quyết định phần nào áp dụng):**
  - **Nhận và sửa (mâu thuẫn thật còn sót từ đợt 6):** LTI 1.3 vẫn mâu thuẫn ở mục 2.7/12/19.2/20/DoD — sửa đồng bộ toàn bộ về REST-API-first; tên thư mục `src/agents/` gây hiểu nhầm multi-agent — ghi chú rõ là tên lịch sử; tiêu đề "Ba vai trò" nhưng bảng có 4 dòng — đổi thành "3 role, 4 persona"; Admin "chỉ xem tổng hợp" chưa đúng khi Admin còn quản lý tài khoản — đổi theo purpose limitation; tiêu đề "Sản phẩm hoàn thiện 100%" là tuyên bố tuyệt đối dễ bị bác — đổi thành "Target release capabilities (baseline v1)".
  - **Nhận, thêm ở mức rẻ (giá trị cao/chi phí thấp):** outcome definition cho risk score (không phải dự đoán dropout, chỉ là nguy cơ trễ kế hoạch tuần); nguyên tắc temporal leakage; tách trạng thái "insufficient data" khỏi "bình thường"; severity-weighted release gate (Critical/High/Medium/Low); stratification tag cho eval case; nguyên tắc dev-set tách khỏi frozen regression set; nguyên tắc deterministic-test-vs-LLM-judge; bảng retention cụ thể theo loại dữ liệu (thay "vô thời hạn"); source precedence khi nhiều nguồn xung đột; wedge decision (thừa nhận Cursus là hybrid, chưa chốt 1 hướng, có chủ đích ở giai đoạn demo); north-star metric bổ sung "weekly learning loop completion"; hạ mức tuyên bố ROI xuống "giả thuyết chưa có số liệu"; tách file này (CHANGELOG.md) khỏi PROJECT_CONTEXT.md.
  - **Ghi nhận là đúng nhưng CHƯA làm ngay, đánh dấu "Production target" thay vì giả vờ đã đủ:** chương trình user research 20-30 phỏng vấn thật; mô hình ROI đầy đủ có biến/best-worst-case; toàn bộ governance cấp doanh nghiệp cho risk policy (four-eyes approval, shadow eval, backtest, drift monitoring); bảo mật cấp production đầy đủ (MFA, CSRF, rate limiting phân tán, SBOM, container scanning...); observability 3 lớp đầy đủ (application/AI/product) — chỉ giữ mức tối thiểu (Sentry + trace correlation ID cho 1 số event chính); hạ tầng production đầy đủ (IaC, CI/CD, blue-green deploy, queue/job system, backup drill); RAG data governance đầy đủ (malware scan, poisoning detection, re-embedding rollback); Mock LMS như contract-test platform đầy đủ (versioned schema, webhook, idempotency); tách toàn bộ docs thành 25+ file theo cây thư mục đề xuất. **Lý do từ chối:** đây chính là dạng "scope creep" mà bản phản biện lần 1 (mục 2.8) đã cảnh báo — làm hết theo chuẩn doanh nghiệp ngay bây giờ sẽ lặp lại đúng lỗi ưu tiên sai đã bị bắt, trong khi dự án còn deadline thật (23/08) và ngân sách 8 triệu. Ghi nhận trung thực là chưa làm, không bịa cho có, để người tiếp theo (kể cả Claude Code) biết chính xác ranh giới demo/pilot vs production.

- **Đợt 8 (18/08/2026) — audit toàn diện + hợp nhất design token màu sắc, thành 1 nguồn sự thật duy nhất trong `frontend/src/index.css`.** Chuyển nguyên văn từ `PROJECT_CONTEXT.md` (mục Ghi chú) về đây theo đúng nguyên tắc ở đầu file này — đây là nhật ký triển khai/implementation detail, không phải yêu cầu sản phẩm.

  **Lý do:** qua nhiều đợt sửa vá rời rạc, codebase tích tụ **6 mã "xanh brand" khác nhau** cho cùng 1 ý định thị giác (`#2468C9`, `#2E7BC4`, `#2F80ED`, `#60A5FA`, `#7DB7FB`, `#38BDF8` — trộn giữa logo, mascot, CTA landing/login, và mảng trang trí), cộng nhiều màu trạng thái/nền hardcode lặp lại rải rác. Đợt này audit toàn bộ component + `index.css` + file locale, chọn **đúng 1 giá trị hex cho mỗi vai trò ngữ nghĩa**, và áp lại toàn repo — không đổi logic nghiệp vụ, không đổi cấu trúc component, chỉ chuẩn hoá token. Bảng tham chiếu:

  | Token | Giá trị | Vai trò / dùng ở đâu |
  |---|---|---|
  | `--brand-blue` | `#2468C9` | Xanh brand hợp nhất (thay cả `#2468C9` cũ lẫn `#2E7BC4`) — logo mark, mắt/ăng-ten mascot (`Mascot.jsx`, `CuriAvatar.jsx`), CTA landing/login/sandbox (`.btn-brand-cta`), khung trích dẫn "verified" (`LandingGroundedQA.jsx`). Chọn theo WCAG 1.4.11 (non-text, ≥3:1) vì phần lớn use-case là đồ hoạ chứ không phải chữ đoạn văn — `#2468C9` đạt 3.31–3.67:1 trên nền tối, `#2E7BC4` thì không. |
  | `--brand-blue-hover` | `#1B57A8` | Trạng thái hover/active của brand-blue (thay `#1D5A94` cũ trong `CuriAvatar.jsx`). |
  | `--brand-blue-text-on` | `#FFFFFF` | Màu chữ/icon đặt TRÊN nền brand-blue (nút CTA, chip). |
  | `--brand-blue-soft` | `rgba(36,104,201,.10)` | Nền nhạt cho badge/chip (chip "verified citation"). |
  | `--brand-blue-glow` | `rgba(36,104,201,.16)` | Bóng đổ/glow bán kính lớn quanh CTA brand-blue. |
  | `--landing-cta-bg` / `--landing-accent` (+ `-hover`/`-soft`/`-glow`) | *(alias)* | Giữ nguyên tên biến cũ để không phải sửa lại toàn bộ JSX đang gọi — nay chỉ trỏ thẳng vào `--brand-blue` tương ứng, không còn giá trị hex riêng. |
  | `--accent` | `#0FA8A0` (light) / `#2DD9CB` (dark) | **Giữ nguyên, không đổi** — màu teal nhận diện riêng của app (dashboard/sau đăng nhập), có chủ đích tách khỏi brand-blue. Dùng cho icon, viền, badge nhạt, các chỗ không phải chữ trắng trên nền màu. |
  | `--accent-cta-bg` | `#0A7268` | **Token mới** — phát sinh khi audit contrast phát hiện `--accent` dùng làm nền nút chữ trắng chỉ đạt 2.95:1 (sáng) / 1.76:1 (tối), trượt chuẩn AA rất xa. Dùng riêng cho nền nút chữ trắng: `.btn-primary`/`.btn-accent` (gradient, ~23 điểm gọi), `.btn-auth-primary` (6 màn hình auth), và ~15 chỗ Tailwind `bg-accent-cta text-white` (App.jsx, AdminConsole, InstructorHome, StudentHome, CuriContextPanel, SemesterSetupWizard, CourseCompanionChat, EmailVerificationScreen, CuriChatLauncher). Đạt 5.80:1 (sáng) / 7.47:1 (tối) với chữ trắng. |
  | `--accent-cta-bg-hover` | `#086054` | Hover của `--accent-cta-bg`. |
  | `--status-info` | *(alias `--brand-blue`)* | Mới đặt tên rõ ràng cho vai trò "info" thay vì dùng ngầm định brand-blue không tên. |
  | `--do`, `--citation`, `--academic-risk` | *(alias `--success`/`--plan`/`--warning`)* | Gộp trùng lặp — trước là hex hardcode giống hệt token gốc, giờ là alias thật để không lệch nhau nếu 1 bên đổi sau này. |
  | `--landing-fixed-surface(-muted)`, `-text(-secondary/-muted)`, `-border`, `-warning` | `#232523`/`#1D1F1D`/`#F1F2EE`/`#B4B8B2`/`#959B94`/`#343834`/`#D3A052` | Token riêng cho khối `LandingGroundedQA.jsx` — khối này **cố tình không đổi theo theme sáng/tối** (1 nhịp thị giác cố định trong bố cục sáng→tối→sáng của landing page), nên tách khỏi các token `--landing-*` đổi theo theme. Nhân tiện sửa `-text-muted` từ giá trị cũ `#858B84` (lỗi, không đạt contrast) thành `#959B94`. |
  | ~~`--gold-glow`~~ | *(đã xoá)* | Xác nhận không còn nơi nào dùng, xoá khỏi cả `:root` và `.dark`. |
  | ~~`.btn-blue`~~ | *(đã xoá)* | CSS rule đặt tên sai (thực chất render màu nâu/vàng `#B7791F` v.v.), xác nhận 0 nơi gọi trong JSX. |

  **Có chủ đích KHÔNG đổi (đã cân nhắc, không phải bỏ sót):** màu trang trí không thuộc nhận diện brand trong `Mascot.jsx` (sách/màn hình/nốt suy nghĩ nổi — đa dạng minh hoạ hợp lệ, không phải brand-blue trùng lặp); nền `#050505` của hero landing (gần đen, không thuộc họ xanh); shadow CTA của `LandingHero.jsx` giữ nguyên literal `rgba(36,104,201,.4/.55)` thay vì `color-mix()` (cú pháp arbitrary-value của Tailwind không ổn định với `color-mix()`); 2 điểm dùng `bg-danger` cho badge/nút xoá chat trong `CuriChatLauncher.jsx` — đã chuyển từ `bg-red-500` sang token `--danger` chuẩn, đạt AA ở theme sáng, còn tồn dư nhẹ ở theme tối (~3.76:1, do `--danger` là màu semantic dùng chung, chưa có biến thể `-cta-bg` riêng) — chưa đủ mức ưu tiên để tạo thêm 1 token mới cho 2 điểm nhỏ này. **Chưa làm (ưu tiên thấp hơn màu, theo đúng yêu cầu):** audit type scale (font-size/weight/line-height) — đã có kết quả audit ghi nhận 6 điểm lệch (H1 `font-black` vs `font-bold` giữa các dashboard, `.landing-section-heading` của `LandingProduct` dùng `clamp()` khác 6 section landing còn lại, nhãn eyebrow/meta lệch 9–13px), nhưng chưa áp dụng sửa.

  **QA bổ sung (axe-core, cả 2 theme, các trang chính + toàn bộ dashboard 3 role): tìm thêm và sửa nốt các vi phạm contrast còn sót, không phát hiện qua audit tĩnh vì chỉ lộ ra khi render thật.** 3 nhóm lỗi thật:
  1. **`--brand-blue`/`--brand-blue-hover` dùng làm CHỮ (không phải đồ hoạ) trên nền tối** — đúng như comment cảnh báo sẵn trong token (brand-blue chỉ đạt 3:1 non-text trên nền tối, KHÔNG đủ 4.5:1 cho chữ nhỏ) nhưng 3 chỗ vẫn lỡ dùng vậy: nút "Nguồn học liệu" trong `LandingGroundedQA.jsx` (2.84:1), link trích dẫn trong `LandingSandbox.jsx`/`LandingWorkflow.jsx` qua `--landing-accent-hover` (2.18:1), và link `mailto:` trong `.legal-content a` (Privacy/Terms, 3.3:1 theme tối). **Sửa bằng 1 token mới `--brand-blue-text-dark: #4A90E2`** (verified 4.61–6.04:1 trên mọi nền tối đang dùng) — `--landing-accent-hover` (theme tối) và `.dark .legal-content a` giờ trỏ vào token này; `--brand-blue`/`-hover` gốc không đổi.
  2. **`--sidebar-text` bị bỏ qua** — 3 chỗ trong sidebar (`App.jsx`) dùng thẳng `text-slate-500` (Tailwind raw) thay vì token `--sidebar-text` đã có sẵn (`#94a3b8`/`#9a9ea8`, 6.96:1) — 3.72–3.74:1, dưới chuẩn. Đổi cả 3 sang `text-sidebar-text` (thêm mapping `--color-sidebar-text` vào `@theme inline`).
  3. **`--plan`/`--do`/`--reflect` dùng làm chữ 9px bold trên chip nhạt** trong `AuthLayout.jsx` (badge PLAN/DO/REFLECT ở 6 màn hình auth) — 3.18:1 (DO, sáng) / 4.09:1 (PLAN, tối). Đổi nhãn chữ của 3 badge này sang `text-fg-secondary` (đã sẵn tương phản cao), viền/nền tint vẫn giữ màu riêng.

- **Đợt 9 (20/08/2026) — đối chiếu lại nhãn ✅/⚠️ ở mục 2.7/4.1/9/14.2/14.4/15 của `PROJECT_CONTEXT.md` với code/test thật, không đổi nhãn nào chỉ vì "nghe hợp lý":**
  - **Verified mới (thêm bằng chứng file:line, giữ ✅):** guardrail chạy trước LLM (`qa_service.py:70-92`); "không tìm thấy → không bịa" (`qa_answer_service.py:65-71,139-144`); seed data không chứa PII thật (`seed_students_SSA101.json:14-17`); payload gọi Gemini (QA/Plan/Reflection) không chứa tên/MSSV thật (`qa_answer_service.py:122-127`, `plan_builder.py:216-221`, `reflection_engine.py:261-265`).
  - **Phát hiện claim sai (không chỉ "chưa verify"):** Sentry **chưa được tích hợp** trong code thật (0 kết quả `sentry_sdk`/`@sentry/*` trong `src/`, `frontend/`, `requirements.txt`, `package.json`) — câu ở mục 7 nói "có Sentry" bị đánh dấu [LỖI THỜI]; hàng "Application logs (Sentry...)" ở mục 14.4 sửa từ "cần verify scrub PII" thành đúng bản chất là chưa có gì để scrub.
  - **Cập nhật tiến độ (không đổi nhãn P0/P1 nhưng thêm nuance):** LLM07 (system prompt leakage) đã có rule `PROMPT_INJECTION` + 1 test pass thật (`guardrail_rules.py:86-104`, `test_gate2_services.py:112-116`) — không còn là "chưa làm gì", nhưng chưa đủ để coi là xong (regex, cần thêm adversarial case). LLM08 (document ingest chưa validate nội dung) xác nhận **vẫn chưa làm** (`document_ingest_service.py:41-57`).
  - **Re-verify không đổi kết luận cũ:** RLS/`tenant_scope.py` vẫn chưa được wire vào route nào, `.env` `DATABASE_URL` vẫn dùng role `postgres` qua pooler (không phải `cursus_app`); eval report vẫn "Generated 2026-08-12", chưa đo lại; `loadtest/` vẫn chỉ có code, chưa có kết quả chạy; `pytest tests/` re-run ra đúng 237 passed/6 skipped/0 failed như 19/08.
  - **Giữ nguyên Unverified theo đúng yêu cầu (claim thị trường, không kiểm chứng được bằng code):** "AI Hay hợp tác FPT", "FPT hiện không dùng Canvas" — đã rà lại, chưa có nguồn mới, không tự suy luận thay.
  4. **`--accent` (teal app, KHÔNG đổi giá trị) dùng làm chữ trên nền sáng** — đạt 2.6–2.79:1, phát hiện ở `DemoSelectRoleScreen.jsx`, rà toàn repo thấy `text-accent` ở 46 chỗ: 26/46 là chữ/link thật → đổi sang token mới `text-accent-text-safe` (alias `--accent-cta-bg` ở theme sáng, alias `--accent` ở theme tối); 20/46 là icon (chỉ cần 3:1 non-text, đã đạt) → giữ nguyên `text-accent`.
  5. Đã cân nhắc nhưng KHÔNG sửa (khác nhóm lỗi contrast): 2 lỗi `button-name` (nút toggle mascot ở `SettingsScreen.jsx` thiếu `aria-label`) và 2 lỗi `label` (2 input ngày ở `SemesterSetupWizard.jsx` thiếu `<label>`) — WCAG 4.1.2/1.3.1, để lại cho 1 đợt riêng.
  6. **Kết quả cuối:** axe-core quét lại toàn bộ trang công khai + dashboard 3 role × 2 theme = 0 vi phạm `color-contrast` còn lại.

axe-core 4.x run against the live Settings page (WCAG 2.0 A/AA + WCAG 2.1 AA rule tags), both light and dark theme.

BEFORE fix:
[AXE:settings-light-en] 1 violation -- "button-name" (critical impact, WCAG 4.1.2 Name/Role/Value):
  the "Floating Cursus Assistant" toggle switch (role="switch") had no accessible name --
  its visible label text lives in a sibling <span>, not associated via aria-label/aria-labelledby,
  so a screen reader announced only "switch, checked" with no indication of what it toggles.
[AXE:settings-dark-en] same violation.

AFTER fix (aria-label added to the switch button, matching its visible sibling label exactly):
[AXE:settings-light-en] NO VIOLATIONS
[AXE:settings-dark-en] NO VIOLATIONS
